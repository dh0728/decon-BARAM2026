# EDA 리포트 — BARAM 2026 데이터 이슈파악

분석 코드: [`eda.ipynb`](eda.ipynb) (동일 폴더). 아래는 해당 노트북 실행 결과를 정리한 요약입니다.

## 결론 요약

| 데이터셋 | 상태 | 핵심 이슈 |
|---|---|---|
| `ldaps_train` / `ldaps_test` | 정상 | 결측치 없음, grid/시각 정합성 완전 |
| `gfs_train` / `gfs_test` | 정상 | 결측치 없음, grid/시각 정합성 완전 |
| `train_labels` | 주의 | `kpx_group_3` 설비용량 초과 38건, `kpx_group_3`는 2022년 전체 결측(33.3%) |
| `scada_vestas_train` | **이슈** | `power_kw10m`에 정상 범위(0~700 kWh/10min)를 크게 벗어나는 초대형 값(최대 ±수천만) 다수 — 센서 오류 의심 |
| `scada_unison_train` | 주의 | 컬럼별 결측치 0.1~1.4%, 풍향 스케일이 VESTAS와 다름(-180~180 vs 0~359) |
| `info.xlsx` | 정상 | KPX그룹별 터빈 수·설비용량이 명세서/타깃 용량과 일치 확인 |

---

## 1. `train_labels.csv`

| 항목 | 값 |
|---|---|
| 크기 | 26,304행 × 4열 |
| 결측치 | `kpx_group_1` 104건(0.40%), `kpx_group_2` 103건(0.39%), `kpx_group_3` 8,766건(33.33%) |
| 중복 행 | 0건 |
| 값 범위 | 0 ~ 21,275 / 21,362 / 21,130 (설비용량 21,600 / 21,600 / 21,000 kWh 이내 대부분) |
| 설비용량 초과 | `kpx_group_1` 0건, `kpx_group_2` 0건, **`kpx_group_3` 38건** |
| 음수 값 | 0건 (전 그룹) |

- `kpx_group_3` 결측(33.3%)은 데이터 명세서에 안내된 대로 **2022년 라벨 미제공**에 의한 것으로, 실제 결측이 아니라 정상적인 제공 범위 차이입니다.
- `kpx_group_3` 설비용량(21,000 kWh) 초과 38건은 실측 오차 수준의 소폭 초과로 추정되며, 학습 라벨은 그대로 두고 **예측값에 대한 용량 클리핑** 적용을 검토할 필요가 있습니다.

## 2. `ldaps_train` / `ldaps_test`

| 항목 | train | test |
|---|---:|---:|
| 크기 | 420,864행 × 35열 | 140,160행 × 35열 |
| 결측치 | 0건 | — |
| 중복 (`forecast_kst_dtm`, `grid_id`) | 0건 | 0건 |
| grid_id 개수 | 16개 (기대치 일치) | 16개 (기대치 일치) |
| 격자 좌표 고정 여부 | 전부 고정 | 전부 고정 |
| `forecast_kst_dtm` 누락 시각 | 0 / 26,304 | 0 / 8,760 |
| `data_available_kst_dtm` 24시간 블록 | 전부 24개 단위로 정상 | 전부 24개 단위로 정상 |
| train/test 컬럼 스키마 | 동일 (차집합 없음) | |

- 구조적 이슈 없음. IQR 기준 이상치 후보 상위 컬럼(`etc_0_VLCDC`, `etc_0_mcc`, `heightAboveGround_2_SWDIR` 등)은 대부분 0에 몰린 분포(운량·복사량)의 특성상 나타나는 자연스러운 결과로, 실제 오류로 보기는 어렵습니다.

## 3. `gfs_train` / `gfs_test`

| 항목 | train | test |
|---|---:|---:|
| 크기 | 236,736행 × 40열 | 78,840행 × 40열 |
| 결측치 | 0건 | — |
| 중복 (`forecast_kst_dtm`, `grid_id`) | 0건 | 0건 |
| grid_id 개수 | 9개 (기대치 일치) | 9개 (기대치 일치) |
| 격자 좌표 고정 여부 | 전부 고정 | 전부 고정 |
| `forecast_kst_dtm` 누락 시각 | 0 / 26,304 | 0 / 8,760 |
| `data_available_kst_dtm` 24시간 블록 | 전부 24개 단위로 정상 | 전부 24개 단위로 정상 |
| train/test 컬럼 스키마 | 동일 (차집합 없음) | |

- LDAPS와 마찬가지로 구조적 이슈 없음. IQR 이상치 상위는 강수 관련 컬럼(`surface_0_prate`, `surface_0_tp`)으로, 강수가 없는 시간이 대부분이라 자연스럽게 발생하는 결과입니다.

## 4. `scada_vestas_train.csv` — 주요 이슈

| 항목 | 값 |
|---|---|
| 크기 | 157,819행 × 37열 (12기 × power/ws/wd) |
| 결측치 | 0건 |
| 타임스탬프 중복/누락 | 0건 (10분 간격 완전) |
| 풍향 범위 | 0.0 ~ 359.0 (전 터빈 공통) |

터빈별 `power_kw10m` 이상 징후:

| 터빈 | 음수 값 개수 | 동일값 최대 연속 길이 | 비고 |
|---|---:|---:|---|
| wtg01 | 24,784 | 494 | |
| wtg02 | 23,751 | 494 | |
| wtg03 | 21,506 | 494 | |
| wtg04 | 26,723 | 2,776 | |
| wtg05 | 27,255 | **7,470** (≈52일 연속 동일값) | 센서 고정(freeze) 의심 구간 |
| wtg06 | 28,489 | 572 | |
| wtg07 | 37,209 | 494 | 12기 중 음수 비율 최고(23.6%) |
| wtg08 | 28,198 | 494 | |
| wtg09 | 27,372 | 494 | |
| wtg10 | 20,141 | 491 | |
| wtg11 | 23,658 | 495 | |
| wtg12 | 28,135 | 513 | |

- 터빈당 `power_kw10m` 최솟값이 **-3천만 ~ -5천만**, 최댓값이 **+3천만 ~ +5천만** 수준까지 관측됩니다. 정격출력 3.6MW(≈600kWh/10min 이하가 정상 범위)로는 전혀 설명되지 않는 규모로, 대기전력 등으로 인한 소폭 음수(-2~-10 수준)와는 별개로 **센서 오류/에러 코드성 극단값**이 섞여 있는 것으로 보입니다.
- wtg05는 동일 값이 7,470행(≈52일) 연속되는 구간이 있어 센서 고정(freeze) 또는 통신 장애 구간일 가능성이 있습니다.
- **권고**: 학습에 SCADA power를 사용할 경우, 정상 범위(예: `|power| > 700` 등 정격 기준)를 벗어나는 값은 결측 처리하거나 클리핑한 뒤 사용해야 합니다. 원본 그대로 사용하면 스케일링·정규화가 심각하게 왜곡됩니다.

## 5. `scada_unison_train.csv`

| 항목 | 값 |
|---|---|
| 크기 | 105,264행 × 16열 (5기 × power/ws/wd) |
| 타임스탬프 중복/누락 | 0건 (10분 간격 완전) |
| 풍향 범위 | **-180.0 ~ 180.0** (VESTAS의 0~359와 스케일 상이) |

| 터빈 | 결측치 | 결측 비율 | 음수 power | 동일값 최대 연속 길이 |
|---|---:|---:|---:|---:|
| wtg01 | 208 | 0.20% | 0 | 4,275 (≈30일) |
| wtg02 | 1,362 | 1.29% | 0 | 523 |
| wtg03 | 352 | 0.33% | 0 | 910 |
| wtg04 | 150 | 0.14% | 0 | 866 |
| wtg05 | 142 | 0.13% | 0 | 1,726 |

- VESTAS와 달리 음수 값은 없지만, 컬럼별로 0.13~1.4%의 결측치가 있어 보간 또는 결측 마스킹 전략이 필요합니다.
- `wtg02_power`는 IQR 기준 이상치 후보가 10,682건(전체의 약 10%)으로 다른 터빈보다 두드러지게 많아 별도 확인이 필요합니다.
- wtg01은 최대 4,275행(≈30일) 연속 동일값 구간이 있어 정지/센서 고정 구간일 가능성이 있습니다.
- **풍향 스케일이 VESTAS(0~359)와 다르므로(−180~180), 두 SCADA를 함께 피처화할 때 각도 변환(sin/cos 등)으로 통일 필요.**

## 6. `info.xlsx`

| 항목 | 값 |
|---|---|
| 크기 | 17행 (터빈 단위) |
| 결측치 | 0건 |

| KPX그룹 | 터빈 수 | 그룹설비용량(MW) |
|---|---:|---:|
| 1 | 6 | 21.6 |
| 2 | 6 | 21.6 |
| 3 | 5 | 21.0 |

- `data_description.md`의 그룹별 설비용량(21.6/21.6/21.0 MW) 및 `train_labels`의 값 범위와 정확히 일치함을 확인했습니다. 그룹1·2는 VESTAS(6기씩), 그룹3은 UNISON(5기) 구성입니다.

---

## 종합 액션 아이템

1. **VESTAS SCADA `power_kw10m` 클렌징 최우선** — 정격 기준을 벗어나는 극단값 처리 없이는 피처로 사용 불가.
2. `train_labels`의 `kpx_group_3` 설비용량 초과 38건 처리 방식 결정(예측값 클리핑 등).
3. UNISON 결측치 보간 전략 수립, VESTAS/UNISON 풍향 스케일 통일.
4. wtg05(VESTAS)·wtg01(UNISON)의 장기 동일값(고정) 구간 원인 확인 — 실제 정지인지 센서 결함인지 구분 필요.
